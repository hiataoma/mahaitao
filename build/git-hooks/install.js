// 'use strict'
// const chalk = require('chalk')

// function exec(cmd) {
//   return require('child_process').execSync(cmd).toString().trim()
// }

// exec('git config core.hooksPath build/git-hooks')
// exec('chmod +x build/git-hooks/commit-msg')

// console.log(chalk.green('Successfully installed git hooks!'))
